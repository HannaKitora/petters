<h1>Event</h1>
<%= link_to 'イベント作成', new_admin_event_path %>

<h1>All Users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
      <%= link_to '削除', admin_user_path(user), method: :delete, data: { confirm: 'Are you sure？' } %> 
    </li>
  <% end %>
</ul>
