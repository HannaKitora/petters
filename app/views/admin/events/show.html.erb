<h1>Event</h1>

<div>
  <h3><strong><%= @event.name %></strong></h3>
  <%= image_tag @event.get_image, size: '400x400' %>
  
  <p>Price:<%= @event.price %></p>
  <p><%= @event.detail %></p>
  
  
    <%= link_to "Edit", edit_admin_event_path(@event.id) %>

    <%= link_to "Destroy", admin_event_path(@event.id), method: :delete, "data-confirm" =>"Are you OK?" %>
 
</div>