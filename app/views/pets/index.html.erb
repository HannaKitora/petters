<h1>Pets</h1>

  <% if user_signed_in? %>
      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th scope="col">name</th>
            <th scope="col">kind</th>
            <th scope="col">caption</th>
          </tr>
        </thead>
        <tbody>
          <% @pets.each do |pet| %>
            <tr>
              <th scope="row">
                <%= link_to pet_path(pet.id) do %>
                  <%= image_tag pet.get_image %>
                <% end %>
              </th>
              <td>
                <%= pet.name %>
              </td>
              <td>
                <%= pet.kind %>
              </td>
              <td>
                <%= pet.caption %>
              </td>
            </tr>
            <tr>
              <th scope="row">
                <%= link_to user_path(pet.user.id) do%>
                  <%= image_tag pet.user.get_profile_image %>
                <% end %>
              </th>
              <th scope="row">
                <%= link_to user_path(pet.user.id) do%>
                  <%= pet.user.name %>
                <% end %>
              </th>
            </tr>
          <% end %>
        </tbody>
      </table>

　<% else %>
      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th scope="col">name</th>
            <th scope="col">kind</th>
            <th scope="col">caption</th>
          </tr>
        </thead>
        <tbody>
          <% @pets.each do |pet| %>
            <tr>
              <th scope="row">
                <%= link_to pet_path(pet.id) do %>
                  <%= image_tag pet.get_image %>
                <% end %>
              </th>
              <td>
                <%= pet.name %>
              </td>
              <td>
                <%= pet.kind %>
              </td>
              <td>
                <%= pet.caption %>
              </td>
            </tr>
            <tr>
              <%= link_to "#{pet.comments.count} Comment", pet_path(pet.id) %>
            </tr>
          <% end %>
        </tbody>
      </table>
  
  <% end %>

