<h1>Post</h1>

<%= form_with model: @pet do |f| %>
  <h4>image</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <h4>pet's name</h4>
  <%= f.text_field :name %>

  <h4>kind</h4>
  <%= f.text_field :kind %>

  <h4>caption</h4>
  <%= f.text_area :caption %>

  <%= f.submit '投稿' %>
<% end %>