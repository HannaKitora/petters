<h1>Pet</h1>

<div>
  <%= image_tag @pet.get_image %>
  <p>名前：<%= @pet.name %></p>
  <p>種類：<%= @pet.kind %></p>
  <p>説明：<%= @pet.caption %></p>
  <p>投稿者画像：<%#= image_tag 'sample-author1.jpg' %></p>
  <p>投稿者：<%= @pet.user.name %></p>
  <p>投稿日時：<%= @pet.created_at.strftime('%Y/%m/%d') %></p>
  <% if @pet.user == current_user %>
    <%= link_to "削除", post_image_path(@pet), method: :delete %>
  <% end %>
</div>